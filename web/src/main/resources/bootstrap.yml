nacos:
  config:
    #    namespace: cloud-dev
    server-addr: 192.168.150.252:8848
spring:
  application:
    name: ch-sso
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.config.server-addr}
        namespace: ${nacos.config.namespace:}
      config:
#        enabled: false
        group: ${spring.application.name}
        server-addr: ${nacos.config.server-addr}
        namespace: ${nacos.config.namespace:}
        shared-configs:
          -
            dataId: ${spring.application.name}.yml
            refresh: true
            group: ${spring.application.name}

