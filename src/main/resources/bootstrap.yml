spring:
  application:
    name: ch-sso
  profiles:
    active: local
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.2:8848
        namespace: ${nacos.namespace:}
      config:
        server-addr: 192.168.1.2:8848
        shared-dataids: ch-sso.yml
        namespace: ${nacos.namespace:}

